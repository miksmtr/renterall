const String name = "mobi";
const String type = "scooter";
const String apiUrl = "https://mobi.envepo.com/api/";
const String icon = "assets/operators/$type/icons/$name.png";

/*

var headers = {
  'Content-Type': 'application/json'
};
var request = http.Request('GET', Uri.parse('https://mobi.envepo.com/api/get-active-devices'));

request.headers.addAll(headers);

http.StreamedResponse response = await request.send();

if (response.statusCode == 200) {
  print(await response.stream.bytesToString());
}
else {
  print(response.reasonPhrase);
}


*/
